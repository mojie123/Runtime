# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LocationComponentization' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
	
  # Pods for LocationComponentization
  pod 'PWRuntime', :path => 'Lib/PWRuntime'
  target 'LocationComponentizationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LocationComponentizationUITests' do
    # Pods for testing
  end

end
